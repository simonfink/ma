MA:Smartventil
--------------

STATUS UPDATES JEDEN FREITAG, an Urs, René, Christian, Adrian, Alejandro
Donnerstag VAT-Tag (Änderungen vorbehalten)

fortlaufende Doku im wiki

info: out of band / in band
-wiki zugang für vat (ach, esa, mara)
-trello einladungen an alle beteiligten

erste schritte:
---------------

-überblick verschaffen
--was hat welche funktion
--protokolle
--Konzept automated testing (inkl. möglichkeit devices zu "sperren")
--was kann rabbitmq? (mqtt, aqmp, stomp,..)

-mindmap, was alles für möglichkeiten
--Protokolle: mqtt, http, coap, aqmp, stomp
--Verschlüsselung
--Kompression
--automated testing


-datenaustausch: aufstellung
--wiki
--git
--svn  --bevorzugt (fabian)
--trello --evtl dazu
--...

mqtt topic: nicht sub-millisec sondern timestamp*1000+submillisec

Arbeitspakete
-------------
-Theoretische Berechnung der gesendeten Pakete (aller Protokolle) (overhead, bandbreite) (abschätzen, rechnen, testen) (analyse)
--anschließende verifizierung durch praktisches beispiel, festhalten, ins wiki
--excel simulator -> berechnet für alle Protokolle
-Kriterien und Gewichtung für Protokolle^
-cloud performance zu lokal
-10Hz, 100Hz, 1kHz, 5kHz,... 
-Welche Programmiersprache?
-Welches Betriebssystem? (anfangs auf RPi, raspbian, rtos rpi...)(+evtl. Einfluss)
-tests
-tests
-tests

im auge behalten:
-----------------
-zeitsynchronisation: ntp, ptp,...
-firewall friendly, iot security
-zeitplan (erst ab oktober)
-hohe datenraten -> kontinuierlich -> empfänger
--kompression, verschlüsselung?
-remote device management (tr-069, lwm2m) (+ welche informationen bei bug, error...)
-RPi - Zeitsynchronisation : alle melden sich um 13:55...

specs:
------
-arm cortex m7 oder besser
-ethernet 100 Mbit
-negotiation feature
-pro ventil:
--5-20 Messgrößen
--kHz Bereich
--adaptives sampling? (Berücksichtigung im Protokoll)

startprojekt:
-------------
-parser mqtt zu cratedb war zu langsam